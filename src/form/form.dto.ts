// for submission
export class FormDto {
  /*
     user id [we already have that i guess]
     form name
     form description
     form fields [in json format]
     submission limit 
     ttl if the user want to set it
     context if user want it 
    */
}
